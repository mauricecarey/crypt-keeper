Add git imports here.
For example:
  git@github.com:mauricecarey/oracle-java-formula.git
  
Then add the import to the minion roots.
For example:
  file_roots:
    base:
      - /srv/salt/imports/oracle-java-formula
